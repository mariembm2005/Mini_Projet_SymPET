<nav
	class="navbar-sympet">

	{# Brand #}
	<a href="{{ path('app_prods1') }}" class="navbar-brand-sympet">
		Sym<span>Pet</span>
	</a>

	{# Links #}
	<ul class="nav-links">
		<li>
			<a href="{{ path('app_prods1') }}">Accueil</a>
		</li>
		<li>
			<a href="{{ path('app_prods1') }}">Nos produits</a>
		</li>
		<li>
			<a href="{{ path('app_commande_show') }}">Commandes</a>
		</li>
	</ul>

	{# Right side #}
	<div
		style="display:flex;align-items:center;gap:12px;">

		{# Search #}
		<form action="{{ path('app_filtrer') }}" method="GET" style="display:flex;align-items:center;gap:0;">
			<div style="position:relative;">
				<i class="fas fa-search" style="position:absolute;left:12px;top:50%;transform:translateY(-50%);color:var(--muted);font-size:12px;"></i>
				<input type="text" name="search" placeholder="Rechercher..." style="
				                        padding: 9px 16px 9px 34px;
				                        border: 1px solid var(--border);
				                        border-radius: 100px 0 0 100px;
				                        background: var(--sage-light);
				                        font-size: 13px;
				                        width: 200px;
				                        outline: none;
				                        font-family: 'DM Sans', sans-serif;
				                        color: var(--charcoal);
				                    ">
			</div>
			<button type="submit" style="
					                background: var(--sage);
					                color: white;
					                border: none;
					                border-radius: 0 100px 100px 0;
					                padding: 9px 16px;
					                font-size: 13px;
					                cursor: pointer;
					                height: 100%;
					            ">
				<i class="fas fa-search"></i>
			</button>
		</form>

		{# Divider #}
		<div style="width:1px;height:24px;background:var(--border);"></div>

		{# Cart #}
		<a href="{{ path('cart_list') }}" class="nav-cart">
			<i class="fas fa-basket-shopping"></i>
			Panier
		</a>

	</div>

</nav>
